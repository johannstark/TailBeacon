[Unit]
Description=TailBeacon Health Check
After=network.target

[Service]
User={{USER}}
Group={{USER}}
WorkingDirectory={{DIR}}
ExecStart={{UV_PATH}} run uvicorn main:app --host 0.0.0.0 --port {{PORT}}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
